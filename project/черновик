CRISP DM - стандарт как с бизнес задачи дойти до ML продукта
TDSP

	Managing Enviroment
python -m venv /path/to/new/virtual/environment - для того чтобы там установить ту версию питона которое нам нужно например чтобы запустить проект на питон который не последняя версия или не та версия что у тебя на компе
venv
virtualenv
poetry
conda miniconda

	Managing dependencies - управления зависимостями
pip 
poetry			<- возможно иммено его нужно уст
pip-tools
Pipfile
conda 

	Modules and packaging
python package structure
build system

pyproject.toml !!

CLI - command line interface

_________________________________________________________

MapReduse <- полезно знать
Docker, Docerfile - Image (образ), Conteiner (установить)   (тупое объяснение Conteiner - мы на сервере или на компе храним несколько контейнерев, в кототрых мы построили раные образы разных библиотек например разного года, для того чтобы на одном компе на разных контейнера можно запустить ранзные проекты разного года реализации)
	docker pull
	docer pc
	docer images
	...
	
прочитать про расширение  .tar

